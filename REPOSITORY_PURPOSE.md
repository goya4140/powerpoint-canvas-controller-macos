# draw-in-powerpoint

> 面向使用者的项目入口、快速开始和视觉样例见 [`README.md`](README.md)；完整分阶段流程见 [`docs/DRAWING_WORKFLOW.md`](docs/DRAWING_WORKFLOW.md)。

## 仓库目的

`draw-in-powerpoint` 是一个面向 Codex 的绘图 Skill，目标是让用户能够直接使用自然语言，在 PowerPoint 中创建、修改和复用论文插图。

本仓库聚焦于生成符合 CCF-A 类人工智能顶级会议论文常见视觉风格的科研图示，包括模型架构图、方法流程图、系统框架图、实验流程图、数据流图和消融实验示意图等。

这里的“CCF-A 类 AI 顶会风格”并非指 CCF 制定了统一的插图规范，而是指 CVPR、ICCV、NeurIPS、ICML、AAAI、ACL 等会议论文中常见的专业、简洁、信息密度适中且适合学术出版的视觉表达方式。

## 核心目标

该 Skill 希望将 Codex 的内容理解、代码生成和任务执行能力，与 PowerPoint 的原生绘图能力结合起来，使用户无需依赖 Figma、Illustrator 等专业设计工具，也能快速制作具有论文发表质量的科研插图。

当前实现以“层级 1 语义骨架”为核心：先用可编辑的线条、图框、文字框、分组与语义箭头确认方法结构，再通过 `asset_slot` 将部分模块升级为层级 2 的图标表达或层级 3 的插画表达。层级升级不得在未经确认的情况下改变已经验证的方法图关系。

生成结果应优先由 PowerPoint 原生对象组成，包括：

- 可编辑的形状、文本框、连接线和箭头；
- 可独立移动、缩放、复制和重新着色的图形元素；
- 可修改字体、字号、线宽、圆角和间距的视觉组件；
- 具有清晰分组、层级和对齐关系的页面结构；
- 可继续由 Codex 或用户在 PowerPoint 中迭代的图稿。

## 要解决的问题

当前许多 AI 绘图工作流依赖 Figma 等专业工具，虽然能够获得可编辑结果，但也增加了工具切换、模板学习和后续维护成本。将绘图流程直接落在 PowerPoint 中，可以降低科研人员的使用门槛，并让生成的图示更容易进入论文写作、组会汇报和答辩演示流程。

本仓库重点解决以下问题：

1. 将自然语言描述转化为结构清晰的科研图示。
2. 避免输出仅能作为整体图片使用的不可编辑结果。
3. 建立适合 AI 论文插图的布局、配色、字体和连线规范。
4. 支持对已有 PPT 图稿进行局部修改，而非每次重新生成整张图。
5. 降低制作复杂模型架构图和方法流程图所需的时间成本。
6. 保证图示在论文、演示文稿以及导出为 PDF 或图片后仍具有良好的可读性。

## 视觉风格原则

生成的插图应遵循以下基本原则：

- **学术性**：视觉表达服务于方法说明，不使用与论文内容无关的装饰。
- **简洁性**：减少冗余元素、过度渐变、复杂阴影和无意义的立体效果。
- **结构性**：通过分组、层级、留白、对齐和视觉动线呈现方法逻辑。
- **一致性**：统一字体、字号、圆角、线宽、箭头、颜色语义和组件间距。
- **可读性**：兼顾彩色屏幕展示、论文缩放、双栏排版及必要的灰度打印场景。
- **可编辑性**：优先使用 PowerPoint 原生矢量对象，避免将整张图扁平化为位图。
- **可复现性**：相同输入和样式约束应尽可能产生结构稳定、便于继续修改的结果。

## 典型使用场景

用户可以通过类似以下的自然语言指令调用该 Skill：

- “根据这段 Method 描述，在 PPT 中画一张模型整体架构图。”
- “把这张草图重绘成适合 CVPR 论文的可编辑方法图。”
- “为编码器、特征融合模块和解码器建立从左到右的数据流。”
- “保持现有布局，只修改第三个模块的名称和内部结构。”
- “将这页方法图统一为低饱和蓝绿色学术配色，并检查对齐和间距。”
- “把当前 PPT 图稿整理成适合双栏论文宽度的横向插图。”

## 预期能力范围

首阶段计划覆盖：

- 从论文摘要、Method 文本、伪代码或结构化描述生成图示；
- 根据草图、截图或参考图重建可编辑的 PowerPoint 图形；
- 创建和维护论文插图常用的模块、容器、箭头与标注样式；
- 对现有 PowerPoint 页面进行增量编辑、排版优化和风格统一；
- 检查遮挡、溢出、错位、连线交叉、字号过小和颜色对比不足等问题；
- 将完成的页面导出为适合论文使用的 PDF、SVG 或高分辨率图片；
- 在交付前同时验证页面视觉效果与对象可编辑性。

## 非目标

本仓库当前不以以下能力为主要目标：

- 替代完整的 PowerPoint 演示文稿生成工具；
- 制作以艺术表现为主的插画、海报或营销视觉；
- 仅生成无法继续编辑的单张 AI 图片；
- 复刻某篇论文的独特视觉资产或受版权保护的图形；
- 保证生成图示能够替代作者对技术内容和论文规范的最终审查。

## 成功标准

当该 Skill 能够稳定完成以下任务时，可以认为仓库达到了核心目标：

1. 用户只需提供技术内容和必要约束，即可获得结构合理的 PPT 论文插图。
2. 图中的主要元素均可在 PowerPoint 中单独选择和编辑。
3. 图示能够清晰表达模块关系、数据流和方法贡献，而非仅追求视觉美观。
4. 输出在常见 AI 顶会论文版式中保持清晰、克制且专业。
5. 用户可以通过后续自然语言指令完成局部调整，而不必从头重绘。
6. 生成流程包含渲染检查和结构检查，能够发现并修正常见排版问题。

## 项目愿景

`draw-in-powerpoint` 希望把 PowerPoint 变成 Codex 可直接操作的科研绘图画布：用户负责表达研究思想，Codex 负责将其组织为专业、清晰、可编辑并适合论文发表的视觉语言。
