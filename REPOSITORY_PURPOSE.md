# 仓库定位

`draw-in-powerpoint` 的长期目标，是把论文 Method 转化为结构清晰、审美克制、可以逐对象编辑的论文插图。

这个目标不再被当作一次 0 → 1 跳跃，而是拆成三个递进阶段：

1. **1 → 1 参考图临摹**：输入一张参考图，重建其结构、版式与视觉语法。
2. **0.5 → 1 受控改写**：输入参考图、对应 Method 文本和改写要求，生成表述相近但风格或排版不同的新图。
3. **0 → 1 原创生成**：输入 Method 文本与用户要求，生成一张或多张完整论文插图。

当前仓库只把第一阶段作为主要成功标准。原因很直接：如果流水线不能稳定临摹一张已经存在的好图，就无法证明它具备处理视觉结构、排版、连接关系、重复图元和学术风格的能力。

## 当前产物

每个 1 → 1 案例包含：

- 可追溯的论文、会议与 Figure 来源；
- 对参考图的分区、模块、连接和视觉图元拆解；
- 一个可验证的 Scene Spec；
- 由 PowerPoint 原生形状、文字和连接线构成的复现页；
- PNG 和 layout JSON；
- 参考图与复现图的左右拼接对比图。

仓库已经用 10 张 2024 年 ICLR、NeurIPS、ACL、ICML、CVPR 和 EMNLP 论文插图建立基准。

## 设计原则

- **先复现结构，再追求细节**：先对齐画布、分区、模块和阅读方向。
- **视觉与逻辑同等重要**：版式相似但箭头错误，不算复现成功。
- **原生可编辑**：关键对象不能被扁平化为整图位图。
- **稳定中间表示**：用 Scene Spec 记录坐标、层级、色彩和对象名称。
- **可比较**：每次构建都生成统一的参考/复现拼接图。
- **可回归**：规格校验和 layout 检查必须可以批量执行。
- **来源透明**：参考图只用于研究、评估和工作流验证，保留论文链接与版权边界。

## 与原 Level 1 工作流的关系

仓库原有的 Level 1 语义图仍然有价值，但它解决的是“方法中有哪些节点和边”，并不能单独回答“参考图为何看起来像现在这样”。

新的 1 → 1 场景规格在 Level 1 语义图之上增加了：

- 精确画布与对象坐标；
- 视觉分区和 z-order；
- 形状轮廓、填充、线宽、圆角和旋转；
- 重复 token、模块栈与网络图元；
- 字体层级与对齐；
- 参考图裁剪和对比输出。

未来 0.5 → 1 会把 Method 的语义图与参考图 Scene Spec 对齐；0 → 1 则会从语义图中预测或检索合适的 Scene Spec。

## 非目标

- 不追求对第三方照片、独特角色或插画的像素级复制；
- 不把论文参考图包装成可复用素材库；
- 不用整张位图冒充“可编辑 PowerPoint”；
- 不把会议名称理解为官方统一视觉规范；
- 不用装饰弥补错误的技术关系；
- 不在没有来源信息的情况下公开展示参考图。

面向使用者的入口、命令和 10 个案例见 [README.md](README.md)，完整流程见 [docs/DRAWING_WORKFLOW.md](docs/DRAWING_WORKFLOW.md)。
